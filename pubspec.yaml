name: rumah_sakit_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # --- Ikon Bawaan ---
  cupertino_icons: ^1.0.8

  # --- FIREBASE UTAMA (Versi Angka) ---
  firebase_core: ^3.10.1 # Versi stabil terbaru yang kompatibel dengan package lain
  firebase_auth: ^5.4.1
  cloud_firestore: ^5.6.1

  # --- TAMBAHAN UNTUK FOTO ---
  firebase_storage: ^12.4.1
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true